name: check_list_front_end
description: A new Flutter application.

version: 1.0.0

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  flutter_spinkit: ^3.1.0
  flutter_bloc: ^0.13.0
  equatable: ^0.2.4
  http: ^0.12.0+2
  shared_preferences: ^0.5.2
  uuid: ^2.0.1
  rflutter_alert: ^1.0.2
  date_format: ^1.0.6
  connectivity: ^0.4.3+2
  provider: ^3.0.0+1



dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - images/